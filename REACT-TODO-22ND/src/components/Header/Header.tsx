import { HeaderContainer, Title } from './Header.Styled';

const Header = () => {
  return (
    <HeaderContainer>
      <Title>CEOS vanilla-todo</Title>
    </HeaderContainer>
  );
};

export default Header;